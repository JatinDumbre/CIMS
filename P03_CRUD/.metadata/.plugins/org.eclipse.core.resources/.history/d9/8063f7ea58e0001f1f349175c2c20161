package com.cims.crud.repositories;

import java.util.Optional;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.stereotype.Repository;

import com.cims.crud.entities.User;

@Repository
public interface SiteOperatorRepository extends JpaRepository<User,Integer>{
	@Query(nativeQuery=true,value="select m.m_name,p1.quantity,u.unit_name,m.description,p2.pm_id"
			+ " from Material_Project p1, Material m, Project_Allocation p2, Unit u"
			+ " where p1.mat_id=m.m_id and p1.pj_id=p_id and m.unit_id=u.unit_id and p2.so_id=:user_id")
	 Optional<Object[]> findMaterialsByProjectManager(int user_id);

}
